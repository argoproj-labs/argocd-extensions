
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: argocdextensions.extension.argoproj.io
spec:
  group: extension.argoproj.io
  names:
    kind: ArgoCDExtension
    listKind: ArgoCDExtensionList
    plural: argocdextensions
    singular: argocdextension
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ArgoCDExtension is the Schema for the argocdextensions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ArgoCDExtensionSpec defines the desired state of ArgoCDExtension
            properties:
              extends:
                description: Extends specifies what part of Argo CD should be extended
                type: string
              source:
                description: Source specifies where the extension should come from
                properties:
                  file:
                    description: File is specified if the extension should be sourced
                      from a URL
                    type: string
                  repository:
                    description: Repository is specified if the extension should be
                      sourced from a git repository
                    properties:
                      name:
                        description: Name specifies the name of the Repository to
                          fetch
                        type: string
                      revision:
                        description: Revision specifies the revision of the Repository
                          to fetch
                        type: string
                    type: object
                type: object
              target:
                description: Target specifies which K8S resource the extension should
                  target
                properties:
                  resource:
                    description: Resource specifies a K8S resource to target
                    properties:
                      group:
                        type: string
                      kind:
                        type: string
                    type: object
                type: object
            required:
            - source
            type: object
          status:
            description: ArgoCDExtensionStatus defines the observed state of ArgoCDExtension
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
